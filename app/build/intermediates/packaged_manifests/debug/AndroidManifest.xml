<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clawd.voice"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <!-- Standard permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- SAMSUNG WORKAROUND: Foreground service permissions -->
    <!-- FOREGROUND_SERVICE is required for all foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FOREGROUND_SERVICE_MICROPHONE is required on Android 14+ for mic access in foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- SAMSUNG WORKAROUND: Wake lock permission -->
    <!-- Allows the service to keep CPU awake while listening, preventing deep sleep -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- SAMSUNG WORKAROUND: Notification permission (Android 13+) -->
    <!-- Required to show foreground service notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- SAMSUNG WORKAROUND: Battery optimization exemption -->
    <!-- Allows app to request exemption from Doze mode -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- SAMSUNG WORKAROUND: Boot receiver permission -->
    <!-- Allows service to restart automatically after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- SAMSUNG WORKAROUND: Exact alarm permission (Android 12+) -->
    <!-- Required for AlarmManager-based service restart mechanism -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Notification Bridge: read notifications from messaging apps -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- SMS Sync: read text messages for context and action items -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <permission
        android:name="com.clawd.voice.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.clawd.voice.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClawdVoice"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.clawd.voice.MainActivity"
            android:exported="true"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.clawd.voice.SettingsActivity"
            android:exported="false"
            android:parentActivityName="com.clawd.voice.MainActivity" />

        <!-- SAMSUNG WORKAROUND: Foreground service with microphone type -->
        <!-- foregroundServiceType="microphone" tells Android this service needs mic access -->
        <!-- This is required on Android 14+ and helps prevent Samsung from killing it -->
        <service
            android:name="com.clawd.voice.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- SAMSUNG WORKAROUND: Boot receiver for auto-start -->
        <!-- Restarts wake word service after device reboot -->
        <receiver
            android:name="com.clawd.voice.BootReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Notification Bridge: listens for SMS/messaging notifications -->
        <!-- User must enable in Settings → Apps → Special access → Notification access -->
        <service
            android:name="com.clawd.voice.NotificationBridge"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" >
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.clawd.voice.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>